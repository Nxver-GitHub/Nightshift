You are assembling the founder's Command Center from Sunday blocks.

Precondition: the brain must already exist (the founding interview is done). If it doesn't, run
`START-HERE.md` first.

Read `blocks/README.md`, present the block menu to the founder, and let them pick. For each chosen
block, follow its `block.md`: install its `code/` + `skill/` into a private `command-center/` in the
founder's brain, wire it to THEIR tools (never hardcode, never write a secret into a file), and start
it in review mode. Install one block at a time and verify it works before the next. Never run a block
on real data without an explicit yes.
